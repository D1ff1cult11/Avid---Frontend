{
  "name": "evm-client-backend",
  "version": "1.0.0",
  "description": "AVID EVM Desktop Client - Main Process",
  "main": "dist/main.js",
  "scripts": {
    "dev": "npm run build && electron .",
    "build": "tsc -p tsconfig.node.json",
    "watch": "tsc -p tsconfig.node.json -w"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "electron-store": "^8.2.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "electron": "^28.0.0",
    "jest": "^30.3.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.2.2",
    "uuid": "^9.0.1"
  }
}
